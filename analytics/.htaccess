RewriteEngine On
RewriteCond %{HTTP_HOST} ^mobile\.cc\.stonybrook\.edu$ [NC]
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,NE,R=301]

#AuthType shibboleth
#Require shibboleth
#ShibRequireSession On
#require affiliation student@stonybrook.edu